// Baixa o JDK do toolchain se ele nao estiver na maquina (mesmo recurso do MUSI).
plugins {
    id("org.gradle.toolchains.foojay-resolver-convention") version "1.0.0"
}

rootProject.name = "ktor-tarefas"
