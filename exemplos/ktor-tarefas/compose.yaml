# Banco para desenvolvimento: docker compose up -d
services:
  banco:
    image: postgres:17-alpine
    environment:
      POSTGRES_DB: tarefas
      POSTGRES_USER: tarefas
      POSTGRES_PASSWORD: tarefas
    ports: ["5432:5432"]
    volumes: [dados:/var/lib/postgresql/data]
volumes:
  dados:
